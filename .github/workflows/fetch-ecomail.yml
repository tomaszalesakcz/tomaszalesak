name: Fetch Ecomail Data

on:
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fetch-data:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Fetch Ecomail data
        env:
          ECOMAIL_API_KEY: ${{ secrets.ECOMAIL_API_KEY }}
        run: node vehiclass/scripts/fetch-data.js

      - name: Commit and push data
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add vehiclass/data/
          git diff --staged --quiet || git commit -m "Update Ecomail data $(date -u +'%Y-%m-%d %H:%M UTC')"
          git push
